---
import { getCollection } from "astro:content";
import type { SlideSchema } from "@content/config";
const slidesCollection = await getCollection("slides");

const slides: SlideSchema[] = slidesCollection[0].data;
---

<section>
  <h3>Slides</h3>

  <ul>
    {
      slides.map((aSlide) => (
        <li>
          {aSlide.description} &nbsp;
          <a href={aSlide.link} class="navlink block" target="_blank">
            link
          </a>
        </li>
      ))
    }
  </ul>
</section>
<style>
  a::after {
    content: "↗";
  }

  .block {
    display: block;
  }

  li {
    margin-bottom: 1em;
  }
</style>
